# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
# dist/ is committed for this project — the built bundle is served
# directly by backend/main.py, and committing it means the app runs
# without requiring Node.js/npm at deploy time (only needed to rebuild
# after editing frontend/src).
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
